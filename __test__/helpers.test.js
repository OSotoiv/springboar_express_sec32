const { getMEAN, getMEDIAN, getMODE, validQuery, saveJSON } = require('../helpers')

test('coming soon', () => {
    expect(1 + 1).toEqual(2)
})
//you need to go back and watch testing videos here
// find out how to open a .json file for writing. is it the same as writing to a .txt file?
//how do you test the entire app? Integration testing....
//how do you test error classes if at all...?